<%= @sendername %> sent this message so you can see the property below.

Try clicking the link.  If that doesn't work in your email client then copy it and
paste it into your browser location bar.

http://<%= @url %>/listings/show/<%= @listingid %>